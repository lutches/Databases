Students (_idnr_, login, name, program)
	program -> program.name

Programs (_name_)

MandatoryProgram (_program_, _course_)
	program -> program.name
	course -> courses.code

Branches (_name_, program)
	program -> programs.name


MandatoryBranch (_branch_,_course_)
	branch -> branch.name
	course -> courses.code

RecommendedBranch (_branch_,_course_)
	branch -> branch.name
	course -> courses.code

BranchIn (_student_, branch, program)
	student -> student.idnr
	program -> program.name
	branch -> branch.name

courses (_code_, credits, name)

courseIn (_course_, department)
	course -> courses.code
	department -> departmentes.name

Departmentes (_name_)

ProgramIn (_program_, _department_)
	program -> program.name
	department -> departmentes.name

Limitedcoursess(_code_, capacity) 
	code -> courses.code

Classification (_name_)

Classified (_course_, _classification_)
	course -> courses.code
	classification -> classification.name

Registered (_course_, _student_)
	course -> courses.code
	student -> student.idnr 

Taken ( _course_, _student_, grade)
	course -> courses.code
	student -> student.idnr 

Waitinglist (_course_, _student_, position)
	course -> limitedcourses.code
	student -> student.idnr

Prerequisite (_course_, _prerequisite_)
	course -> courses.code
	prerequisite -> courses.code
